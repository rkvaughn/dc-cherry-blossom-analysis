## TODO file: notes i want to address when my claude credits reset

1. update the legend on the figure so that the ensemble bounds are correctly labeled as a shaded region instead of a dotted line
2. update the legend so the bloom dates icon is correctly shows as a flower rather than a circle